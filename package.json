{
  "name": "es2015_demo",
  "version": "1.0.0",
  "description": "to be published npm module test",
  "main": "index.js",
  "scripts": {
    "test": "npm run compile && mocha --compilers js:babel-core/register",
    "compile": "babel src -d lib",
    "prepublish": "npm run compile"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.16.0",
    "mkdirp": "^0.5.1",
    "request": "^2.79.0"
  }
}
